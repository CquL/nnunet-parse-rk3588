# Expected Private Files

Copy this file from the protected model store:

```text
h100_rknn64_build_package/nnunetv2_PARSE_model_minimal/Dataset501_PARSE/nnUNetTrainer__nnUNetPlans__3d_fullres/fold_0/checkpoint_best.pth
```

The checkpoint is intentionally not stored in git or release assets.

